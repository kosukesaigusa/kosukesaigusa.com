name: Deploy to Cloudflare Pages

on:
  push:
  workflow_dispatch:
  schedule:
    # 毎日午前 0 時に実行する
    - cron: '0 0 * * *'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up bun
        uses: oven-sh/setup-bun@v1

      - name: Sync r2-image-worker secrets
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: tools/r2-image-worker
          wranglerVersion: '4.61.1'
          command: deploy --minify ./src/index.ts
          secrets: |
            USER
            PASS
        env:
          USER: ${{ secrets.IMAGE_WORKER_USER }}
          PASS: ${{ secrets.IMAGE_WORKER_PASS }}

      - name: Generate markdowns
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          NOTION_CONTENTS_DB_ID: ${{ secrets.NOTION_CONTENTS_DB_ID }}
          NOTION_POSTS_DB_ID: ${{ secrets.NOTION_POSTS_DB_ID }}
          NOTION_TALKS_DB_ID: ${{ secrets.NOTION_TALKS_DB_ID }}
          IMAGE_WORKER_USER: ${{ secrets.IMAGE_WORKER_USER }}
          IMAGE_WORKER_PASS: ${{ secrets.IMAGE_WORKER_PASS }}
          IMAGE_WORKER_BASE_URL: ${{ secrets.IMAGE_WORKER_BASE_URL }}
        run: |
          export IMAGE_WORKER_AUTH_TOKEN="$(printf '%s' "${IMAGE_WORKER_USER}:${IMAGE_WORKER_PASS}" | base64 -w 0)"
          bun i
          bun run generate
        working-directory: ./tools/notion-content

      - name: Build
        run: |
          bun i
          bun run build
        working-directory: .

      - name: Publish to Cloudflare Pages
        id: publish_to_cloudflare_pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: kosukesaigusa
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          wranglerVersion: '4.61.1'

      - name: Add publish URL as commit status
        uses: actions/github-script@v6
        with:
          script: |
            const sha = context.payload.pull_request?.head.sha ?? context.sha;
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              context: 'Cloudflare Pages',
              description: 'Cloudflare Pages deployment',
              state: 'success',
              sha,
              target_url: "${{ steps.publish_to_cloudflare_pages.outputs.url }}",
            });
